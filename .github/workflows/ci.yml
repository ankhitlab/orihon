name: Orihon CI

on:
  push:
  pull_request:

jobs:
  node-22-compatibility:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run typecheck

  unit-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run typecheck
      - run: npm run docs:check
      - run: npx playwright install --with-deps chromium
      - run: npm run test:browser
      - run: npm run test:leaks
      - run: npm run test:plugin
      - run: npm run size

  browser-matrix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium firefox webkit
      - run: npm run build
      - run: npm run test:e2e
      - name: Upload browser diagnostics
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
          retention-days: 7

  # CPU performance contract: median of several runs vs committed baseline.
  # Threshold defaults to 15% (runners are noisy). GPU paint benches stay manual /
  # dedicated-runner only — see bench/README.md.
  performance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npm run perf:ci
      - name: Upload perf results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: perf-cpu-ci
          path: bench/results
          if-no-files-found: ignore
          retention-days: 14
